# Moneta for iPhone

Open Moneta.xcodeproj and run the Moneta scheme.
The initial shell establishes a buildable SwiftUI app; feature views follow in separate PRs.

Run the repository quality scripts from the root. CI compiles Release for Simulator.
Tests activate when the test targets land. No signing is needed for Simulator builds.
